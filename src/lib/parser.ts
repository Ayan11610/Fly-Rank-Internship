import { Vulnerability } from "@/types/vulnerability";

export function parseCodeAST(code: string, filepath: string): Vulnerability[] {
  // Simple foundational stub for static AST or regex analysis parsing.
  // Real implementation will scan code string in Phase 2.
  return [];
}
